---
title: Introduction
description: Self-hosted project management for 3D-printing models
---

# Print Vault

Print Vault is a self-hostable management platform for `.3mf` files. Its
structure mirrors [MakerWorld](https://makerworld.com) or
[Printables](https://www.printables.com) — just completely self-hosted: your
models, print profiles and documents live in your own Postgres database and
S3-compatible storage.

The whole catalog is private: every page requires sign-in, so a paid or
personal model collection never leaks. Editing is collaborative — any
signed-in user may edit models and collections, while destructive actions stay
with the owner (and [moderators/admins](/roles)).

## Features

- **Model management** — upload one or more `.3mf` files per model; title,
  description, images, and printer tags auto-fill from embedded file metadata
- **Catalog** — browsable homepage grid with collections; dedicated model
  detail page with per-file downloads
- **Search** — fuzzy (typo-tolerant) matching across models and collections,
  with filters (type, uploader, printer, filament, nozzle diameter, print
  time) and sort
- **Collections** — user-organized groups of models with an "Add to
  collection" action on model pages
- **Bill of Materials** — per-model item list (name, quantity, optional
  link/image); CSV upload in the creation wizard and download via the model
  page
- **PDF documents** — attach build instructions or manuals; viewed inline in
  the browser and downloadable
- **Markdown descriptions** — GitHub-flavored markdown; raw HTML is never
  rendered
- **Platform import** — import metadata, images and files from
  [MakerWorld and Printables](/integrations/makerworld-printables), including
  whole MakerWorld collections
- **Onshape integration** — [import models from Onshape
  documents](/integrations/onshape) via OAuth2, sync when the document
  changes, or pin an immutable version snapshot
- **Print estimates** — an optional headless-slicer service estimates print
  time and filament use from uploaded `.3mf` files
- **Parametric OpenSCAD models** — upload or import a `.scad` source and
  generate customized `.3mf` files from its customizer parameters via the
  optional headless OpenSCAD service
- **Open in slicer** — open files directly in OrcaSlicer or Bambu Studio as an
  alternative to downloading
- **Authentication** — email/password with optional [OIDC single
  sign-on](/sso); all pages require sign-in
- **User roles** — [moderators and admins](/roles) for cleanup and user
  management

## Where to go next

- [Self-hosting](/self-hosting) — run your own instance with Docker Compose
- [Configuration](/configuration) — every environment variable in one place
- [Integrations](/integrations) — connect Onshape, Bambu Cloud, MakerWorld
  and Printables
